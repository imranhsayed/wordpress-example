/**
 * Internal dependencies
 */
import { OneNovantaLoadMore } from './load-more';

// Initialize all Load More wrappers when DOM is ready.
document.addEventListener( 'DOMContentLoaded', () => OneNovantaLoadMore.init() );
