/**
 * Import the Slider component from the web-components package.
 * This component can be reused across multiple blocks.
 * To include it, add the following to your block's `viewScript`:
 * "viewScript": "one-novanta-web-component-slider"
 */

/**
 * External dependencies
 */
import '@rtcamp/web-components/build/slider';
